# 3 分钟演示脚本（城市管网资产数字化台账 · 智能预测与 AI 诊断）

本脚本用于答辩 / 汇报现场演示，串起完整的业务闭环：
**① 传感器实时 → ② 模型预测某管段 7 天后风险 → ③ Agent 自然语言解析依据 → ④ 一键推送 / 建单 → ⑤ 反馈闭环**。

演示不需要 DeepSeek API Key，也不需要联网：AI 助手在无 Key 时自动降级为「后端本地诊断引擎」（意图规划器 + 真实工具调用），结论、规范引用、处置建议照常输出，只是文案由模板拼装而非大模型生成。

---

## 0. 演示前准备（提前 10 分钟做完）

### 0.1 同步代码 + 启动后端

**先在 Windows 宿主机（PowerShell）把改动同步到虚拟机**：

```powershell
scp -r "F:\延大大实习-23大数据-资料同步\02项目资料（底座原型）\项目二（企业级）\dataBase\dataBase\src\python\*" lixuan@192.168.220.129:~/dataBase/src/python/
scp "F:\延大大实习-23大数据-资料同步\02项目资料（底座原型）\项目二（企业级）\dataBase\dataBase\output\show.html" lixuan@192.168.220.129:~/dataBase/output/
```

**再在虚拟机里启动**：

```bash
cd /home/lixuan/dataBase
mkdir -p logs
python3 -m py_compile src/python/main.py src/python/agent_brain.py src/python/kb_rag.py \
                      src/python/predict_ledger.py src/python/predictive_models.py   # 无输出即语法通过
pkill -9 -f "uvicorn.*main" 2>/dev/null
cd src/python
nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 > ~/dataBase/logs/fastapi.log 2>&1 &
sleep 8 && curl -s http://localhost:8000/health
```

`sleep 8` 不要缩短——加载模型要点时间，5 秒时会误报「连接被拒绝」，其实只是还没监听。

### 0.2 接口自检（逐条应返回 JSON）

**注意：这些接口都要鉴权**，不带 token 会全部 401。中文查询参数必须让 curl 自己编码，
直接往 URL 里塞中文会被 h11 断开连接、返回空 body，看着像后端挂了其实是请求没发出去。

```bash
B=http://127.0.0.1:8000
TOKEN=$(curl -s -X POST $B/auth/login -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"admin123"}' \
  | grep -o '"token":"[^"]*"' | cut -d'"' -f4)
echo "TOKEN 长度: ${#TOKEN}"        # 为 0 说明登录失败，后面全部会 401
AUTH="Authorization: Bearer $TOKEN"

curl -s $B/kb/stats -H "$AUTH" | head -c 200; echo              # docs / vocabulary > 0
curl -sG $B/kb/search --data-urlencode "q=球墨铸铁管检测周期" -H "$AUTH" | head -c 300; echo
curl -s $B/predict/summary -H "$AUTH" | head -c 300; echo       # model_ready: true
curl -s $B/predict/metrics -H "$AUTH" | head -c 200; echo
curl -s $B/predict/dashboard -H "$AUTH" | head -c 200; echo
curl -s $B/predict/diagnose/SENSOR-001 -H "$AUTH" | head -c 400; echo
curl -s -X POST $B/agent/chat -H "$AUTH" -H 'Content-Type: application/json' \
     -d '{"message":"未来风险最高的管段是哪段"}' | head -c 500; echo
```

**判据**：
- `kb/stats` 的 `docs` 为 0 → 先在页面上点「📚 重建知识库索引」；
- `predict/summary` 的 `model_ready` 为 `false` → 先点「♻ 回流重训」并等它跑完（见 §4）；
- `predict/summary` 里 `risk_score` 全为 `0.0` → **此时还没跑传感器模拟，属正常**，按第 ① 幕灌够 25 帧再看；
- `kb/search` 返回空 body → 十有八九是中文没编码，改用上面的 `-G --data-urlencode` 写法。

### 0.3 浏览器

打开 `http://<虚拟机IP>:8000/output/show.html`，用 **admin / admin123** 登录（super_admin，所有写操作按钮可见）。
建议 F11 全屏、缩放 100%，提前把「智能预测」页打开一次让 ECharts 完成初始化。

---

## 1. 时间轴总览

| 幕 | 内容 | 页面 | 时长 |
| --- | --- | --- | --- |
| ① | 传感器实时采集 | 传感器监控 | 30s |
| ② | 模型预测 7 天后风险 + 热力 / 趋势 / RUL / 关键因子归因 | 智能预测 | 50s |
| ③ | AI Agent 自然语言诊断 + RAG 规范引用 + 多轮追问 | 智能预测 | 50s |
| ④ | 一键建工单 / 推送预警 | 智能预测 → 预警管理 | 30s |
| ⑤ | 处置反馈回流 → 重训 → 指标量化 | 智能预测 | 20s |

---

## 2. 逐幕演示

### 第 ① 幕 · 传感器实时（30s）

**操作**：左侧导航 →「传感器监控」。工具栏依次设置：

1. `间隔` → **3s**（回放速度，演示时每 3 秒推进一帧）
2. `采样周期` → **14 小时**　★关键设置，见下方说明
3. `场景` → **混合异常**
4. 点 **▶ 开始模拟**

右上角徽标应变为：`● 模拟运行中 · 场景 mixed · 第 N 帧 · 采样周期 14 小时`。

**帧数是硬指标，但只影响「智能预测」页，不影响「传感器监控」页**：

| 帧数 | 传感器监控页 | 智能预测页 |
| --- | --- | --- |
| **第 1 帧起** | 立即就有 **2~3 段故障 + 0~1 段预警**（各段起点已错开，不用预热） | **风险全是 0.0**、`top_features` 为空、RUL 是个固定值——模型 `window=15` 凑不满滑动窗口时走的默认分支，**不是 bug** |
| 15 帧（45s） | 状态开始轮换（故障段修复、新的管段进入预警） | 开始出数 |
| **25~30 帧（75~90s）** | — | **推荐演示点**：风险榜 / 热力图 / RUL 都有内容；**趋势线此时只有 1 个点**（只跑过第 20 帧那一轮扫描） |
| 60 帧（3min） | 走完一个完整劣化周期，回到第 1 帧附近的状态 | 第 3 轮扫描，趋势线开始成线 |
| **200 帧以上** | — | **台账口径刷新点**：10 轮扫描写满 2000 条，重训前的旧记录被完全顶掉（见兜底表「面板指标像是旧模型的」） |

赶时间就先把 `间隔` 设成 1s 快速灌帧，灌够 25 帧再改回 3s 做正式演示。

> **⚠ 「⏩ 单步」喂不出台账、成果面板和趋势线**
> 闭环扫描 `_prediction_scan` 只挂在后台模拟循环里，**每 20 帧自动触发一次**；
> 而 **⏩ 单步** 调的是 `/sensors/sim/tick`，它只生成读数，**不触发扫描、不写台账**。
> 所以：单步连点能喂出风险榜 / 热力图 / RUL（这三样是请求时实时算的），
> 但**预测成果面板、趋势线、台账表格**必须靠 **▶ 开始模拟** 真跑起来才会有新数据。
> 想快就把 `间隔` 设成 1s —— 200 帧约 3.5 分钟，正好把台账刷成当前模型的成绩。

> **哪些管段会劣化（固定 9 段，覆盖全部 5 种管型）**
> `SENSOR-001 / 013 / 025`（供水）、`037 / 049`（燃气）、`061 / 073`（供暖）、`085`（污水）、`097`（危废输送）。
> 它们**持续劣化**而非逐帧随机跳变（随机尖峰会被 15 帧滑动窗口平均掉，模型学不到东西），
> 起点按 7 帧均匀错开，所以任一时刻都有管段处在不同阶段，风险榜有明确梯度、不会并列。
> 其余 91 段只有约 **0.7%** 的随机异常读数——异常是稀有事件，这样「预测未来会异常」才有信息量。

> **榜首会轮换，别背编号**
> 每段管子的周期是 60 帧：20 帧劣化 → 12 帧故障保持 → 28 帧抢修复位。
> 所以**风险榜第一名随时间变化**，第 ② 幕上台时**看当下榜首是谁就讲谁**，
> 记下它的 `sensor_id` / `asset_id` 带到第 ③ ④ 幕即可。
> 好处是**不用重启后端也能反复看到渐进劣化**——每 60 帧（3s 间隔下约 3 分钟）自动循环一轮。
> 场景选 `正常运行` 时不产生劣化管段，全网健康，可用来做前后对比。

> **`tick` 不清零，但这已经不影响演示**
> `tick` 是进程级累加的，「⏹ 停止」再「▶ 开始模拟」**不会**把它清零。
> 因为劣化是周期性的，从任意 tick 起看都是完整的循环，所以中途停/开没有副作用。
> 唯一会被重启清掉的是 `sensor_history`（只在内存里，启动时**不会**从 `data/sensor_history.jsonl` 回读）——
> 重启后要重新灌够 15 帧预测页才会出数。**别在临上场前重启后端。**

> **★ 为什么必须选「采样周期 14 小时」**
> 模型固定 `horizon = 12 步`（预测未来 12 帧）。`12 步 × 14 小时 = 168 小时 = 7.0 天`，
> 这样页面上的「预测跨度」才会显示 **7.0 天**，与讲稿里「预测未来 7 天风险」严丝合缝。
> 「间隔 3s」只是演示回放速度，不参与任何时长折算——这两个概念在系统里是分开的字段
> （`interval_sec` vs `sample_period_sec`），可以现场点出这一设计细节。

**口播词**：
> 「先看数据入口。这里模拟的是管网现场传感器，每 3 秒回放一帧。要注意的是，一帧代表现场多长时间是可以配置的——我们设成 14 小时一帧，因为后端预测模型的预测跨度是固定的 12 步，12 步 × 14 小时正好是 7 天，这样模型输出的就是真实的『未来一周风险』。」

---

### 第 ② 幕 · 模型预测（50s）

**操作**：左侧导航 →「🧠 智能预测」。

1. **指向右上角徽标**，此时应显示：
   `模型已就绪 · 预测跨度 7.0 天（horizon=12 步 × 采样周期 14.0 小时/步）`
2. **预测成果面板**（页面第一张卡）——念出四个指标：命中率、精确率、误报率、平均提前预警时长；
   下方一行是「模型 vs 规则阈值基线」同口径对比。
   **念之前先花 10 秒核对这四个数字是「当前这个模型」的成绩**：它们来自 `data/prediction_ledger.json`，
   是历史累积的，**重训和重启都不会把它清零**。核对方法见兜底表「面板指标像是旧模型的」。
3. **预测风险热力（区域 × 管网类型）**——指出颜色最深的格子。
4. **预测趋势线（按闭环扫描轮次）**——横轴是帧号，一轮闭环扫描一个点；指出风险分随轮次的起伏。
   图上只有 1 个点，说明模拟还没跑够 40 帧（2 轮扫描）——把 `间隔` 设 1s 多跑一会儿再讲这张图。
5. **RUL 剩余寿命预估**——显示的是量级档位（`< 1 小时` / `≈ N 小时` / `≈ N 天`），**不是跳秒倒计时**；指一下颜色最深（最紧急）的那个资产。
   别慌「TOP5 里有好几个都是 `< 1 小时`」——这是**诚实的**：模型目标是整数步，1 步 = 1 个采样周期，它本来就分不出 0.1 步和 0.9 步；差别在每行右侧灰色小字的原始步数上。想让档位拉开，去「传感器监控」把 `采样周期` 调大（如 86400 = 1 天）。
   口播只说「还剩多少步**内**会出事」，不要念成精确倒计时（见第 ⑤ 幕 Q&A：RUL 头在未删失样本上 R² 为负，能分远近、不能逐步解读）。
   值得指的一点是**自洽性**：同一张卡片上的「异常概率」和「RUL」不会再互相打脸——概率高的必然 RUL 小，概率低的必然 RUL ≥ horizon。
6. **未来风险榜 TOP10**——榜首**会随时间轮换**（9 段劣化管段各自错开 7 帧循环），
   **当场念第一名是谁**，记下它的 `asset_id`，第 ③ ④ 幕要用。
   合理性校验：榜首应当落在这 9 段里 —— `SENSOR-001 / 013 / 025 / 037 / 049 / 061 / 073 / 085 / 097`。
   如果不是，说明帧数还不够（<15 帧）或场景选成了「正常运行」。

**口播词**：
> 「这一页是预测成果面板。上面四个数字是模型在这本台账上的真实战绩：命中率、精确率、误报率、平均提前多久发出预警——它们和下面的规则阈值基线是同一本台账、同一套前向窗口口径算出来的，所以这个对比是可信的，不是各说各话。
> 这里我特别点一下『平均提前预警』这张卡：模型提前多少步、规则基线多少步，两个数并排写着。基线的逻辑是『已经越限了才报警』——它报警那一刻异常已经在发生了，所以它量出来的那点提前量不是预测出来的，只是异常会持续多帧、评估窗口往后看又撞上了同一段异常而已；真正的代价是所有还没越限的管段它一律漏掉。模型是在异常发生**之前**报警——这就是它的业务价值，也是为什么单看精确率会看偏。
> 左边热力图按区域和管网类型交叉看风险集中度，右边趋势线按闭环扫描轮次画平均风险分和预警数的走势，横轴是帧号。
> 中间这个 RUL 是模型输出的剩余步数乘采样周期折算出的时间量级，每次数据刷新重算。我们**故意只显示档位、不做秒级倒计时**——RUL 头能可靠分清「马上要出事」和「还早」，但给不出精确到分钟的剩余寿命，做成跳秒倒计时就是对观众过度承诺。
> 下面这个 TOP10 就是未来 7 天最危险的管段。」

---

### 第 ③ 幕 · AI Agent 自然语言诊断（50s）

**操作**：滚动到「AI 诊断助手」卡片（右下「知识库检索」旁边）。

1. 点快捷标签 **「未来风险最高的管段」**（等价于输入
   `未来风险最高的管段是哪段？为什么？建议动作？`）→ 回车。
   回复是**四段式**：① 预测结论 ② 为什么是它（**关键因子归因**，卡片标题旁会标出实际用的方法）③ 规范依据（带 `[1][2]` 编号 + 出处）④ 处置建议。
   方法标签有三种，含义不同、别念混：`SHAP（局部）` 和 `遮挡归因（局部）` 解释的是**这个点位**为什么被判高风险；`全局特征重要度（非局部）` 只说明训练集里哪些特征普遍有用，是历史不足 3 帧时的降级结果，卡片上会自动打⚠。演示 VM 装了 shap，正常显示的是 `SHAP（局部）`。
2. **连续追问**（体现多步推理与记忆，不要重新输入编号）：
   - `它的规范依据是什么` ← 「它」自动解析为上一轮诊断的那个资产
   - `那第三段呢` ← 序号自动解析为风险榜第 3 名
3. 点快捷标签 **「球墨铸铁管检测周期依据」** → 走 RAG 检索，回复下方列出**引用卡片**（标题 / 出处 / 相关度）。
4. 右下角「知识库检索」输入框直接输入 `球墨铸铁管检测周期`，点 **检索**，展示原文片段、分类、`doc_id`。

卡片底部有一行引擎说明，指给评委看：
`当前引擎：后端本地诊断（意图规划器 + 真实工具调用）。在 data/agent_config.json 填入 DeepSeek API Key 即切换为大模型。`

**口播词**：
> 「这是 AI 诊断助手。我问它『未来哪段风险最高、为什么、建议怎么做』——它不是查数据库念数字，而是自己决定调用哪些工具：先调预测，再调知识库检索，最后把四段结论组织出来。
> 关键是第三段『规范依据』，每条都带编号和出处，这是 RAG 向量召回的结果，不是模型编的。知识库用 TF-IDF 字符 n-gram 建的向量索引，运维手册、国标、还有已闭环工单沉淀下来的处置经验都在里面。
> 再演示多轮：我说『它的规范依据』——『它』指代的是上一轮诊断的那段管网；我说『第三段呢』——它知道是风险榜第三名。这靠的是会话记忆加指代消解。
> 底部这行是引擎状态。现在没配 API Key，走的是后端本地诊断引擎，功能完全一样；把 DeepSeek 的 Key 填进配置文件就自动切成大模型 function calling，前端不用改一行代码。」

---

### 第 ④ 幕 · 一键建单 / 推送（30s）

**操作**：

1. 在「关键因子归因 · AI 诊断」输入框里填第 ② 幕记住的编号（如 `SENSOR-001`），点 **诊断**。
   诊断结果底部有两个按钮：**📋 据此建工单** 和 **🤖 让 AI 解释**。点 **📋 据此建工单** → 弹出确认 → 成功提示带工单号。
2. 回到 AI 助手，输入 `把这个风险推送给运维负责人`。
   Agent 会先**复述将要执行的动作并要求确认**（两步确认，防误触发真实网关）→ 再输入 `确认推送` → 执行。
3. （可选，时间充裕时）导航 →「预警管理」，找到 `来源=预测` 的那条预警，点 **推送**，
   展示推送记录里的真实状态：`已发送` / `已发送（模拟）` / `发送失败：原因`。

> **注意**：真实短信 / 企业微信网关需要 AccessKey，现场没配就选「模拟发送」，
> 记录照样落库到 `data/push_records.json`。接入方式见 `推送网关接入说明.md`。
> 「业务闭环策略」卡里的 **自动推送真实网关** 开关演示时**保持关闭**，否则会真发短信。

**口播词**：
> 「诊断完了不能停在结论上，要能落地。这里一键建工单，工单里自动带上风险分、关键因子和规范依据。
> 推送这块我特意做了两步确认——因为后端对接的是真实的阿里云短信和企业微信网关，Agent 不能自己偷偷把短信发出去，必须先复述动作、等我确认。
> 预警管理页能看到推送记录的真实状态。没配网关的时候选模拟发送，记录一样落库，不影响演示。」

---

### 第 ⑤ 幕 · 反馈闭环与量化（20s）

**操作**：

1. 滚动到「预测台账与处置反馈」表格。每行有四个反馈按钮：**确认异常 / 误报 / 已处置 / 无需处置**。
   点 **确认异常** → 在弹窗里填一句处置说明 → 确定。提示：`反馈已回流，当前已标注样本 N 条`。
2. 表格「真实结果」列会从 `待评估` 变成 `命中` / `误报` / `漏报` / `正确判正常`。
3. 回到页面顶部，点 **♻ 回流重训** → 后台异步跑，`/predict/retrain/status` 可查进度 → 完成后模型热加载。
4. 再看一遍「预测成果面板」的指标变化。

**口播词**：
> 「最后闭环。运维人员在台账上标注这次预测到底准不准——确认异常、误报、已处置、无需处置。这些人工结论就是下一次重训的真实标签。
> 点回流重训，后台异步跑，跑完模型热加载，不用重启服务。这样系统就不是一个静态模型，而是『预测 → 预警 → 推送 → 工单 → 反馈 → 重训』的完整闭环，越用越准。」

---

## 3. 收尾一句话（10s）

> 「整条链路的技术要点：预测模型是不依赖 GPU 的轻量梯度提升 + 滑动窗口特征工程；知识库检索用 TF-IDF 字符 n-gram 向量索引，没有引入任何新的第三方向量数据库；Agent 支持大模型 function calling 和本地降级双引擎；所有推送走后端真实网关并留痕；预测效果和规则基线在同一本台账上同口径对比。整套东西在一台虚拟机上就能完整跑通。」

---

## 4. 兜底方案（现场出问题怎么办）

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| **风险全是 0.0**、`top_features` 空、RUL 是个固定值 | 模拟帧数不足 15 帧，模型走的是无数据默认分支（`window=15` 凑不满滑动窗口） | 回「传感器监控」点 **⏩ 单步** 连点到 25 帧以上，或把 `间隔` 改 1s 快速灌帧；确认 `场景` 不是「正常运行」（该场景不生成劣化管段） |
| **重启后端后风险又全归零** | `sensor_history` 只在内存里，启动时**不会**从 `data/sensor_history.jsonl` 回读，重启即清空 | 重启后必须重新跑够 25 帧再切到预测页。所以**别在临上场前重启后端**；真要重启，留出 1~2 分钟灌帧 |
| 徽标显示「模型未就绪」 | 没训练过 / 训练失败 | 点 **♻ 回流重训**，等 1~3 分钟；期间先讲第 ③ 幕的知识库检索和 AI 助手（这两块不依赖模型） |
| 预测跨度不显示「7.0 天」 | 采样周期没设成 14 小时 | 回「传感器监控」把 `采样周期` 改为 **14 小时**，重新点 **▶ 开始模拟**，再回预测页刷新 |
| 台账空表 / 一直不更新 | 闭环扫描只挂在 **▶ 开始模拟** 的后台循环里，每 20 帧触发一次；**⏩ 单步不触发扫描、不写台账** | 点 **▶ 开始模拟** 真跑 200 帧以上（`间隔` 设 1s 约 3.5 分钟）。单步连点对台账无效 |
| **面板指标像是旧模型的**（精确率远低于基线、误报率 80%+、记录里 `prob` 全是 1.0、趋势线只有 1 个点） | 台账里存的还是**重训前**那个模型写的记录。台账按条数保留（最近 2000 条 = 10 轮扫描），**重训和重启都不会清它** | 浏览器开 `/predict/ledger?limit=5`，比对记录的 `time` 与 `/predict/metrics` 里的 `last_retrain.time`：记录更早就是旧账。点 **▶ 开始模拟** 跑满 **200 帧**（10 轮扫描）把旧记录顶掉，面板自动换成当前模型的成绩 |
| AI 回复带「后端不可达」 | uvicorn 挂了 | `tail -n 50 ~/dataBase/logs/fastapi.log` 看报错，重启服务 |
| AI 回复没有引用编号 | 知识库索引为空 | 点 **📚 重建知识库索引**，看提示里的条数和向量维度 |
| 建工单 / 推送按钮不见了 | 当前账号无写权限 | 用 admin / admin123 登录（super_admin） |
| 点推送真的发出了短信 | 闭环策略里开了自动推送 | 演示前确认「自动推送真实网关」**未勾选**；手动推送时选「模拟发送」 |
| 页面白屏 / 图表不渲染 | 静态资源或 ECharts CDN | 检查是否通过 `/output/show.html` 访问；CDN 不通时用本地 ECharts |
| 重训报 **「标签全是同一类」** | 采数据时跑的是纯 `故障`/`异常` 场景（正样本率≈100%），或压根还没产生过异常（≈0%） | 场景切回 **混合异常**，跑满 **60 帧**（一个完整劣化周期）再点重训 |
| 重训报告 **AUC≈0.50** | 训练数据里缺少「持续劣化型」样本，全是逐帧独立随机尖峰 | `/predict/retrain/status` 的 `warnings` 字段会直接写明原因和该调哪个参数，先看它再动手 |
| **avg_precision 很高但 AUC≈0.5** | 正样本率过高，模型只是背了基准率——`avg_precision` 的基准线就等于正样本率 | 看报告里的 `label.positive_rate_test`：健康值约 **12%**；若 >60%，说明采数据时场景选成了 `故障` 而不是 `混合异常` |

---

## 5. 可能被问到的问题

**Q：没有 API Key，这算 AI 吗？**
A：算。Agent 的核心是「意图规划 + 工具编排 + 结论组织」，这套逻辑在后端本地引擎里完整实现了，调的是真实的预测接口和真实的向量检索。大模型只是让文案更自然、能处理更刁钻的问法——配上 Key 立即切换，前端零改动。这个降级设计本身就是工程化的一部分：不能因为一个外部依赖挂掉，整个演示就崩了。

**Q：命中率和规则基线怎么保证是同口径？**
A：两者写进同一本预测台账（`predict_ledger`），用同一套前向窗口判定逻辑评估：预测发出后，往后看 12 帧内是否真的出现异常，出现即 `hit`，没出现即 `false_positive`；窗口过期还没结果的记 `expired`，不计入分母。提前预警时长就是「预测帧」到「真实异常帧」的步数差乘采样周期。

**Q：要是规则基线的精确率比模型还高，那模型的价值在哪？**
A：先分清两者的**角色不同**。规则基线是**同期检测器**——它只在「当前这一帧已经越限」时才报警；而本系统里的劣化是持续多帧的（20 帧劣化 → 12 帧故障保持），所以它一报警，往后 12 帧大概率仍在异常，几乎必然记 `hit`，精确率自然好看。它的代价是**漏报**：所有还没越限的管段一律看不见。
那基线卡上那个「平均提前 N 步」怎么解释？那不是预测能力——它报警的那一刻异常**已经在发生**，之所以还量得出几步提前量，是因为异常会持续多帧，评估窗口往后看时又撞上了同一段异常的后续帧而已。模型的提前量是真·提前：报警时异常还没出现。当前这版把判正阈值接到了训练扫出的 F1 最优点（见下一条 Q&A），命中率 / 精确率 / 平均提前三项对基线全胜，现场照面板念即可；但**最硬的一条仍是提前量**——把「平均提前预警」这张卡上模型和基线两个步数当场并排念出来，比任何单项百分比都有说服力。

**Q：模型「判正」的阈值 0.8 是哪来的？为什么不是 0.5？**
A：不是拍的，是训练时扫出来的。`train_predictive.py` 在**测试集**上把阈值从 0.1 扫到 0.9、取 F1 最高的那点，写进 `models/predictive_report.json` 的 `best_f1_threshold`，并随模型一起存进 `predictive_meta.json`；`online_predict` 拿它当 warning 判正线，critical 线取它到 1.0 的中点（0.8 → 0.9）。台账口径是「warning 及以上即判正」，所以这个阈值直接决定面板上的命中率 / 精确率 / 误报率——同一批台账记录实测：阈值 0.35（旧的写死值）精确率只有四成，抬到 0.8 后八成以上。现场核对工作点：`GET /predict/risk/SENSOR-001` 的响应里带 `threshold` 字段，就是当前生效的那个值。
　　代价要主动说，别等评委问：阈值抬高后**每轮预警条数变少、平均提前量也会缩短**（报警点更贴近异常发生点），换来的是误报率大幅下降。这是精确率与召回率的正常权衡，不是模型变差了。想换口径（比如「宁可错报」）只能改 `predictive_meta.json` 里的 `threshold` 或重训——推理端没有旁路，`online_predict` 的 `risk_threshold` 参数只在模型没存阈值时才兜底。

**Q：为什么用 TF-IDF 不用 Embedding 模型？**
A：约束是不引入新的第三方依赖、离线可跑。管网运维文本是强领域术语（「球墨铸铁」「阴极保护」「有限空间作业」），字符级 n-gram 对这类术语的召回反而比通用 Embedding 稳。索引持久化到 `models/kb_index.joblib`，重建一次几秒。要换成向量模型，只要替换 `kb_rag.py` 里的编码函数，接口不变。

**Q：预测跨度为什么是 12 步？**
A：`window=15, horizon=12` 是模型的固定超参——用 15 帧历史预测未来 12 帧。要改预测天数，改的是采样周期这个业务参数，不是重新训模型，这也是为什么现场能立刻从「12 小时」切到「7 天」。

**Q：你怎么证明模型真学到了东西，而不是背了个基准率？**
A：训练报告里 `avg_precision` 旁边一定会打印**正样本占比**，因为 `avg_precision` 的基准线恰好就等于正样本占比——两者接近就说明模型什么都没学到，只有 **AUC** 不受基准率影响、是唯一可信的判别指标。报告还会自动输出 `warnings`，目前覆盖六类：①训练/测试正样本率背离 >35%（采集期间切换过场景）②正样本率 >60% ③正样本率 <2% ④AUC<0.55 ⑤RUL 自洽约束被破坏 ⑥RUL 头无绝对预测力。每条都直接写明原因和该调哪个参数，不用另写脚本去查。
　　这里有个容易踩的坑值得主动讲：标签是「未来 12 帧内是否异常」，单帧异常率 `p` 会被放大成 `1-(1-p)^12` 的正样本率。所以**异常必须是稀有事件**——实测（VM 跑满 120 帧 / 12000 条）：整体异常读数占比 **4.37%**，其中随机背景 **0.89%**、9 段劣化队列 **39.5%**；正样本率全量 **13.00%**（训练集 13.58% / 测试集 10.77%），落在真实预测性维护数据集的常见区间。可学习的正样本主要来自那 9 段**持续劣化**的管段，而不是逐帧独立的随机尖峰。随机尖峰在数学上就是不可预测的，拿它当正样本只会把 AUC 拉到 0.5。
　　*（现场按实际报告念数：`GET /predict/retrain/status` → `history[0].tail`，或直接看 `models/predictive_report.json`。注意是 `tail` 不是 `detail.tail`——`add_retrain_record` 会把 detail 整个摊平到记录顶层。）*

**Q：五种管网的指标量纲差那么多（燃气压力 0.1 MPa、供暖流量 150 m³/h），模型怎么通用？**
A：模型是跨管型 pooled 训练的，所以特征工程里除了原始量纲的滑动统计，还额外给每个指标算了一份**因果 expanding z-score**——用该传感器自己 0..t 的历史均值和标准差归一化，再在归一序列上算趋势斜率。这样「相对自身常态偏离了多少、正在以多快的速度偏离」在所有管型上是同一个量纲，一个阈值就能通用。
　　关键是必须用 `expanding`（只看过去）而不是整列的 `mean/std`：后者含未来信息，会让离线指标虚高、上线掉分。这一步的必要性我实测量化过（VM 上 12000 帧 / 100 传感器 / `horizon=12`，每个传感器取 `*_zslope15` 里判别力最强的那个特征算单特征 AUC）：

| 归一化方式 | 劣化队列（9 段） | 随机背景（46 段） |
| --- | --- | --- |
| `expanding` 因果（上线用的） | **0.834** | 0.689 |
| 整列 `mean/std`（泄漏未来） | 0.940 | 0.670 |
| 差值 | **+0.106 虚高** | −0.018 |

　　虚高恰好出现在有真实信号的劣化队列上（+0.106），背景段基本不动——因为整列统计把「这段管后来确实劣化了」这个未来事实提前喂给了归一化。上线时只能拿到过去，所以必须用因果版；拿泄漏版的 0.940 去汇报，评委一追问就穿。
　　*（顺带纠正一个我早先写错的说法：随机背景并不是「始终在 0.50 附近」。实测 0.689，说明背景段的趋势特征也携带部分信号，不是纯不可预测噪声。）*
